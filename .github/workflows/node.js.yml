# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions.

name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:   
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
#     - run: npm ci
#     - run: npm i -g @testim/testim-cli
    - run: npm i -g @testim/testim-cli && testim --token "3H6KvG710VRtis8e0j4nbH0Lc5MJIZzhCYsSjm2DOEENb7uNYk" --project "rMDuwZcHfy1l6jUu7cXo"  --grid "Testim-Grid" --reporters junit -r ~/junit.xml --testId "4So4099xDBdk8Kpz"
    
    - uses: mikepenz/xray-action@v0.9.4
      with:
        username: ${{ secrets.client_id }}
        password: ${{ secrets.client_secret }}
        testFormat: "junit"
        testPaths: "~/junit.xml"
       # testExecKey: “XRAYD-14”
#         testPlanKey: "XRAYD-196"
        continueOnImportError: false
        projectKey: "XRAYD"
